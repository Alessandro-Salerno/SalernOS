#!/bin/sh

# Configuration
CONFIG_DEBUG_ON=/dev/tty6
CONFIG_WM_WAIT=1
CONFIG_WM_PATH=jwm

Xorg -dumbSched > ${CONFIG_DEBUG_ON} 2> ${CONFIG_DEBUG_ON} &
(sleep ${CONFIG_WM_WAIT} && ${CONFIG_WM_PATH} > ${CONFIG_DEBUG_ON} 2> ${CONFIG_DEBUG_ON}) &
