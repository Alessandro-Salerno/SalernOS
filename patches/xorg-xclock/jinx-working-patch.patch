diff '--color=auto' -uNr xorg-xclock-clean/Clock.c xorg-xclock-workdir/Clock.c
--- xorg-xclock-clean/Clock.c	2022-04-07 22:39:49.000000000 +0200
+++ xorg-xclock-workdir/Clock.c	2025-02-06 20:57:41.668353916 +0100
@@ -1425,6 +1425,7 @@
     char *time_ptr;
     register Display *dpy = XtDisplay(w);
     register Window win = XtWindow(w);
+    w->clock.update = 60.0f;
 
     X_GETTIMEOFDAY(&tv);
     tm = *localtime(&tv.tv_sec);
@@ -1433,7 +1434,6 @@
             XtAppAddTimeOut(XtWidgetToApplicationContext((Widget) w),
                             waittime(w->clock.update, &tv, &tm),
                             clock_tic, (XtPointer) w);
-
     round_time(w->clock.update, &tm, &tv);
     /*
      * Beep on the half hour; double-beep on the hour.
